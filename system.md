# СИСТЕМА FX-АНАЛИЗА — файл для ввода в курс дела

Версия документа: 2026-09-14.
Владелец: Сергей. Ведёт документ: Claude.

---

## 0. ЕСЛИ ТЫ — CLAUDE В НОВОМ ЧАТЕ, НАЧНИ ОТСЮДА

Владелец прислал ссылку на этот файл, потому что предыдущий чат закончился.
Порядок действий:

1. Прочитай этот файл целиком.
2. Забери самый свежий пакет данных. Список файлов:
   `https://api.github.com/repos/suragby-ui/fx-daily/contents/archive`
   Возьми последний по имени (формат `daily_ГГГГ-ММ-ДД_ЧЧММ.md`) и прочитай его
   по адресу из поля `download_url`.
3. В пакете лежат: метод разбора, журнал прогнозов и ошибок, бары и свинги по
   девяти инструментам на шести таймфреймах, позиционирование CFTC, ставки и
   волатильность.
4. ОБЯЗАТЕЛЬНО прочитай в пакете раздел «Паттерны ошибок» ДО того, как что-то
   анализировать. Там перечислено, на чём система уже ошибалась.
5. Новостей в пакете нет — их добирай поиском в момент разбора.
6. После этого можно работать. Владелец обычно пишет «отчёт на сегодня».

Резервный адрес текущего пакета (может отдаваться из кэша, поэтому предпочитай
список архива):
`https://raw.githubusercontent.com/suragby-ui/fx-daily/main/daily.md`

---

## 1. ЧТО ЭТО ЗА СИСТЕМА

Ежедневный разбор валютного рынка по методу из мастер-промта
FINANCIAL WORLD MODEL AI. Суть метода: идти от состояния мира к цене, а не
наоборот. Сначала состояние каждой валюты, потом их столкновение, потом
позиционирование участников, и только в конце — уровни и вероятности.

Система построена в сентябре 2026 года за несколько дней. Это рабочий
прототип восприятия для большого проекта Financial World Intelligence (FWI),
техпроект которого лежит отдельно у владельца.

Инструменты: EURUSD, GBPUSD, USDJPY, USDCHF, AUDUSD, NZDUSD, USDCAD, XAUUSD,
плюс DXY, если брокер его отдаёт.

---

## 2. ЧТО ВЫДАВАТЬ ВЛАДЕЛЬЦУ

Формат отчёта утверждён 14.09.2026 и без его слова не меняется. Главный
результат — таблица трёх сценариев:

| Инструмент | Цена | ↑ | ↓ | ⟷ | Отмена ↑ (ниже) | Отмена ↓ (выше) | Отмена ⟷ (вне) |

Три вероятности в сумме дают 100. Горизонт — неделя. Флет = цена в пределах
одного дневного ATR. Отмена срабатывает ТОЛЬКО по закрытию свечи H4 за уровнем
и необратима. Полное описание правил — в разделе «Как разбирать этот пакет»
внутри самого пакета.

Плюс к таблице: головной вывод, короткие блоки по инструментам, календарь
событий, запись прогнозов в журнал.

---

## 3. ЖЕЛЕЗО И ФАЙЛЫ

**Сервер:** Windows, Timeweb, Москва, IP российский. Заходит владелец по RDP,
пользователь Administrator. Приглашение в консоли `PS C:\Users\Administrator`.
Если видишь `PS C:\Users\user` и русский интерфейс — это его домашний
компьютер, там ничего нет.

**Python:** `C:\Cerberus\venv\Scripts\python.exe` (общий с торговым проектом
ЦЕРБЕР, там уже стоят MetaTrader5, pandas, certifi).

**Папка системы:** `C:\FXReport\`

| Файл | Что делает |
|---|---|
| `fx_report.py` | тянет бары из MT5, считает свинги, подволны, ATR, стохастик, фибо |
| `market_context.py` | тянет COT с www.cftc.gov и ставки/волатильность с FRED |
| `run_daily.ps1` | запускает оба, склеивает пакет, зовёт выгрузку |
| `publish.ps1` | выгружает пакет на GitHub через Contents API |
| `analysis_spec.md` | описание метода, едет внутри пакета |
| `journal.md` | журнал тезисов, прогнозов и ошибок, едет внутри пакета |
| `system.md` | этот файл |
| `publish.json` | настройки GitHub: owner, repo, branch, path |
| `gh_token.txt` | токен GitHub, в чат НИКОГДА не присылать |
| `out\` | результаты: latest.md, context.md, daily.md, архив, лог |
| `cache\` | годовые архивы CFTC, перекачиваются не чаще раза в 20 часов |

**Расписание:** задача `FXReportDaily`, каждые 30 минут, старт в 00:05,
от имени Administrator. Пакет всегда не старше получаса.

Ручной запуск, если нужен прямо сейчас:
```
powershell -NoProfile -ExecutionPolicy Bypass -File C:\FXReport\run_daily.ps1
Select-String -Path C:\FXReport\out\run_daily.log -Pattern "LINK:" | Select-Object -Last 1
```

**GitHub:** пользователь `suragby-ui`, публичный репозиторий `fx-daily`,
ветка `main`. Токен fine-grained, права Contents: Read and write, бессрочный.
Архивные копии старше четырёх дней чистятся автоматически.

---

## 4. ЧТО В ПАКЕТЕ ЕСТЬ И ЧЕГО НЕТ

**Есть:** цены и бары со всех таймфреймов от MN1 до M15; свинг-точки с
размером ноги и процентом отката; подсчёт подволн вторым зигзагом (пятёрка =
импульс, тройка = коррекция); уровни Фибоначчи; ATR и Stoch(5,3,3);
позиционирование CFTC по категориям с трёхлетними процентилями; доходности
США, инфляционные ожидания, кредитный спред, VIX, GVZ, OVX; широкий индекс
доллара.

**Нет, и об этом надо говорить вслух в каждом разборе:**
- перекос опционов (risk reversal, butterfly, структура сроков) — OTC-данные,
  только платный вендор;
- институциональный поток заявок и дилерские книги;
- кривые OIS;
- новости и события — добираются поиском отдельно;
- ряд EVZ мёртв с марта 2025, помечается в таблице как устаревший;
- COT выходит по пятницам и отражает позиции на вторник: лаг три дня.

---

## 5. ГРАБЛИ, НА КОТОРЫЕ УЖЕ НАСТУПАЛИ

**Кодировка PowerShell.** Windows PowerShell 5.1 читает `.ps1` без BOM как
ANSI. Любая кириллица в скрипте ломает разбор. Все `.ps1` пишутся только
латиницей и сохраняются с BOM и переводами строк CRLF. Русский текст живёт
в `.md`-файлах.

**Кодировка вывода.** `tasklist` на русской Windows отдаёт cp866. Декодировать
через latin-1 с errors=ignore, иначе падает.

**Российский IP.** `publicreporting.cftc.gov` отдаёт 403. При этом
`www.cftc.gov` и FRED открыты — поэтому COT берётся годовыми архивами
с основного сайта, а не через API.

**Сертификаты.** Хранилище Windows не содержит части корневых сертификатов;
используется набор из certifi. Есть аварийный ключ `FXCTX_INSECURE=1`.

**Кэш GitHub.** Стабильный адрес `daily.md` может отдаваться из кэша по
многу минут. Поэтому для чтения используется список архива и датированные
имена файлов.

**Терминал MT5.** Скрипт НИКОГДА не запускает терминал сам: `mt5.initialize()`
поднимает его, а это уже приводило к аварии с дублями. Перед подключением
проверяется наличие процесса terminal64.exe; если его нет — выход с
сообщением. Ордера не выставляются, чужие процессы не трогаются.

**Полные пути.** Команды владельцу давать только с полными путями: он
запускает их из разных папок и с разных машин.

---

## 6. КАК ОБЩАТЬСЯ С ВЛАДЕЛЬЦЕМ

- По-русски, кратко, без длинных вступлений.
- **Говорить обо всём, даже о мелочах.** Это его прямое требование от
  14.09.2026, после того как я умолчал о размере позиции управляющих по евро
  и он получил убыток. Ничего не фильтровать как незначительное.
- Правки кода — полными файлами на замену, не кусками.
- Команды — готовыми блоками для копипаста, с полными путями.
- Перед изменением файла — бэкап: `copy file file.bak_<что>_<дата>`.
- После изменения — проверка через Select-String с ожидаемым числом совпадений.
- Правильность с первого раза важнее скорости.
- Честные негативные выводы он принимает, если приложен конкретный следующий
  шаг. Ошибки называть ошибками прямо.
- Не предлагать паузы и не звать «закончить на сегодня».
- При работе в чужом интерфейсе (GitHub, СБИС и т.п.) — строго один шаг на
  сообщение, следующий после «сделал» или скриншота.
- Проценты не округлять в красивую сторону. 55 против 45 — это узкий перевес,
  так и говорить.

---

## 7. ЧЕСТНО О РЕЗУЛЬТАТАХ

На 14.09.2026: закрытых тезисов восемь, из них три верных и пять неверных.
Все восемь ставок от 12.09 пошли против прогноза — но это была фактически
одна ставка «доллар вниз», выраженная в восьми парах, то есть примерно
полтора независимых наблюдения, а не восемь.

Выборка мала, выводов о наличии преимущества делать нельзя. Договорённость:
когда в журнале наберётся около двадцати независимых эпизодов, считаем
калибровку по-настоящему. Если окажется, что направленные оценки хуже
монетки — правильный вывод не «переворачивать их», а перестать их выдавать
и оставить только описание состояния рынка и уровни.

Не обещать доходность. Не изображать уверенность, которой нет.

---

## 8. СВЯЗАННЫЕ ПРОЕКТЫ ВЛАДЕЛЬЦА

- **ЦЕРБЕР** — торговый робот на MT5, живёт на том же сервере в `C:\Cerberus`.
  Ничего из его файлов и задач не трогать.
- **FWI (Financial World Intelligence)** — большой проект собственного
  финансового ИИ, техпроект v1 у владельца на руках. Эта система —
  работающий прототип его слоя восприятия.

---

## 9. ЧТО ОБНОВЛЯЕТСЯ САМО, А ЧТО РУКАМИ

Автоматически каждые 30 минут: бары, свинги, подволны, позиционирование,
ставки — всё это внутри пакета.

Руками, то есть Claude выпускает новый файл и владелец кладёт его в
`C:\FXReport\`:
- `journal.md` — после разбора, когда появились новые прогнозы, закрылись
  тезисы или вскрылась ошибка;
- `analysis_spec.md` — когда меняются правила метода;
- `system.md` — когда меняется устройство системы.

Выгрузка на GitHub идёт сама, файл заливается только если его содержимое
изменилось.
